version: "3.11"

services:
  api:
    retry: 5
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 8000
    ports:
      - 8000:8000

  web:
    build: nginx
    ports:
      - 80:80
    depends_on:
      - api
